// backend/services/t_entity_crud/src/controllers/update/xxx.update.controller.ts
/**
 * Purpose: Per-route controller for PATCH /api/xxx/v1/:xxxId (no-op stub).
 */
/*
import type { RequestHandler } from "express";
import { ControllerBase } from "@nv/shared/base/ControllerBase";
import { CtxKeys } from "@nv/shared/http/HandlerContext";

export class XxxUpdateController extends ControllerBase {
  constructor() {
    super({ service: "t_entity_crud" });
  }
  public handle(): RequestHandler {
    return super.handle(async (ctx) => {
      ctx.set(CtxKeys.ErrStatus, 501);
      ctx.set(CtxKeys.ErrCode, "not_implemented");
      ctx.set(
        CtxKeys.ErrDetail,
        "Update Xxx is not implemented yet. Next: load DTO, apply setters, WAL-first, then DB save."
      );
    });
  }
}
*/
