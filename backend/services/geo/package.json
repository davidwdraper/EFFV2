{
  "name": "geo-service",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "start": "ts-node -r tsconfig-paths/register --project tsconfig.json index.ts",
    "dev": "TS_NODE_FILES=1 ts-node-dev -r tsconfig-paths/register --respawn --transpile-only index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  }
}
