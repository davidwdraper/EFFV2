{
  "name": "@nv/gateway",
  "version": "0.1.0",
  "private": true,
  "_nvHeader": "SOP header: auth service. CJS runtime. Uses @nv/shared via alias.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "NODE_OPTIONS='-r tsconfig-paths/register' tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node -r tsconfig-paths/register dist/index.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^20.12.12",
    "tsx": "^4.19.2",
    "tsconfig-paths": "^4.2.0"
  }
}
