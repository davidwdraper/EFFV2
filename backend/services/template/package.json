{
  "name": "template-service",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "start": "ts-node -r tsconfig-paths/register --project tsconfig.json index.ts",
    "dev": "yarn build && TS_NODE_FILES=1 ts-node-dev --respawn --transpile-only index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^7.6.0",
    "pino-http": "^10.5.0"
  },
  "devDependencies": {
    "@types/express": "4.17.21",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  }
}
